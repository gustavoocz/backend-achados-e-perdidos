spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/achados_e_perdidos?useTimezone=true&serverTimezone=UTC
    username: root 
    password: abc123 
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: update
    show-sql: false